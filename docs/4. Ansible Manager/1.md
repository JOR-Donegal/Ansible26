# Installation

On ansiblemanager, I need to install Ansible. I add Ansible's PPA to the list of sources and update them.
```
sudo apt-add-repository ppa:ansible/ansible
sudo apt update
```
And then install the package.
```
sudo apt install ansible -y
```
